import React from "react";
import ReactDOM  from "react-dom";   //for react version 17
//import { createRoot } from 'react-dom/client';

import App from "./App";

ReactDOM.render(<App />, document.getElementById('root')); //for version 17
//createRoot(document.getElementById('root').render(<App />));